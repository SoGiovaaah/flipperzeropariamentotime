import {useState} from 'react'
import Router from 'next/router'
export default function Register(){const[u,setU]=useState('');const[p,setP]=useState('');const[c,setC]=useState('');const submit=async(e:any)=>{e.preventDefault();const r=await fetch('http://localhost:3001/api/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p,confirm:c})});if(r.ok){Router.push('/login')}};return(<form onSubmit={submit} className="min-h-screen flex flex-col items-center justify-center bg-gray-900"><input className="m-2 p-2" value={u} onChange={e=>setU(e.target.value)} placeholder="User"/><input type="password" className="m-2 p-2" value={p} onChange={e=>setP(e.target.value)} placeholder="Password"/><input type="password" className="m-2 p-2" value={c} onChange={e=>setC(e.target.value)} placeholder="Confirm"/><button className="bg-blue-500 text-white p-2">Register</button></form>)}
