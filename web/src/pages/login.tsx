import {useState} from 'react'
import Router from 'next/router'
export default function Login(){const[u,setU]=useState('');const[p,setP]=useState('');const submit=async(e:any)=>{e.preventDefault();const r=await fetch('http://localhost:3001/api/login',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})});if(r.ok){const j=await r.json();localStorage.setItem('token',j.token);Router.push('/profile')}};return(<form onSubmit={submit} className="min-h-screen flex flex-col items-center justify-center bg-gray-900"><input className="m-2 p-2" value={u} onChange={e=>setU(e.target.value)} placeholder="User"/><input type="password" className="m-2 p-2" value={p} onChange={e=>setP(e.target.value)} placeholder="Password"/><button className="bg-blue-500 text-white p-2">Login</button></form>)}
